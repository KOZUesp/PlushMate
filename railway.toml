[build]
builder = "nixpacks"

[deploy]
startCommand = "gunicorn app:app --workers 1 --threads 4 --timeout 120 --bind 0.0.0.0:$PORT --ssl-version 2"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
