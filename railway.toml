[build]
builder = "nixpacks"

[deploy]
startCommand = "gunicorn app:app --workers 1 --threads 4 --timeout 120 --limit-request-line 8190 --bind 0.0.0.0:$PORT"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
