[build]
builder = "nixpacks"

[deploy]
startCommand = "gunicorn app:app --workers 1 --threads 4 --timeout 60 --bind 0.0.0.0:$PORT --log-level debug --capture-output"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
