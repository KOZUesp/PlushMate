[build]
builder = "nixpacks"

[deploy]
startCommand = "gunicorn app:app --log-level debug --capture-output --enable-stdio-inheritance --workers 1 --threads 4 --timeout 60 --bind 0.0.0.0:$PORT"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
